[package]
name = "kamu-money-pg-artifact"
version = "0.1.0"
description = "Closed artifact verification for the kamu-money-pg release harness."
authors.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
sha2 = "0.11"
tempfile = "3"

[lib]
path = "src/lib.rs"

[[bin]]
name = "kamu-money-pg-artifact"
path = "src/main.rs"
